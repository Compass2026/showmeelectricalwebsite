import type { Redirect } from "@/config/redirects";

/** Harbor Lane is a new (fictional) site: no legacy URLs, no redirects, nothing gone. */
export const redirects: Redirect[] = [];
export const gone: string[] = [];
